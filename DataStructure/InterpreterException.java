package DataStructure;

public class InterpreterException extends Exception {
    public InterpreterException(String errorType) { //throwable error add in after
        super(errorType);
    }

    //remember to add ERROR keyword
}
